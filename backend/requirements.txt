# =============================================================
#  Old Document Digitizer — Python Requirements
# =============================================================
#
#  Platform notes:
#  - Tested with Python 3.10 / 3.11
#  - PaddlePaddle requires Python 3.8–3.11 (NOT 3.12+)
#  - TensorFlow 2.x requires Python 3.8–3.11
#  - On Windows use paddlepaddle (CPU). On Linux/CUDA use paddlepaddle-gpu.
#
#  Install:
#    pip install -r requirements.txt
#
#  GPU (Linux + CUDA 11.x):
#    pip install paddlepaddle-gpu
#    (instead of paddlepaddle)
# =============================================================

# ── Web framework ─────────────────────────────────────────────
flask>=3.0,<4.0
flask-cors>=4.0,<5.0
werkzeug>=3.0,<4.0

# ── Production WSGI server ────────────────────────────────────
gunicorn>=22.0

# ── OCR engine ───────────────────────────────────────────────
# CPU-only (default). Replace with paddlepaddle-gpu for GPU servers.
paddlepaddle>=2.6,<3.0
paddleocr>=2.8,<3.0

# ── Image processing ──────────────────────────────────────────
opencv-python>=4.8,<5.0
numpy>=1.24,<2.0

# ── Machine learning (CRNN model) ────────────────────────────
# TensorFlow 2.x — CPU-only build is fine for CRNN inference.
# On Apple Silicon: use tensorflow-macos instead.
tensorflow>=2.13,<3.0

# ── LLM / Ollama client ───────────────────────────────────────
ollama==0.6.2
